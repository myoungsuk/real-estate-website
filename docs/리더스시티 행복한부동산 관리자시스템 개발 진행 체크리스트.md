# 리더스시티 행복한부동산 관리자시스템 개발 진행 체크리스트

## 2026-08-26 네이버 매물 운영 변경

- [x] 관리자 매물 목록을 네이버 공개 매물 50건 기준으로 전환
- [x] 매물명·유형·매물번호 검색과 매물유형 필터, 결과 수 `aria-live` 제공
- [x] 부동산뱅크 EUC-KR HTML-table `.xls` 브라우저 가져오기·개인정보 제거·diff 미리보기 구현
- [x] 그 밖의 네이버 매물 직접 등록·수정·등록 종료와 카드 미리보기 구현
- [x] `naver-listings.json`을 Access·CSRF·최신 SHA·공개 콘텐츠 검증을 거치는 관리자 허용 리소스로 연결
- [x] 엑셀에 없는 기존·수동 매물 자동 삭제 금지와 기존 공개 층 정보 보존
- [x] 관리자 대시보드 통계를 네이버 공개 매물 기준으로 교체
- [x] 공개 매물 가짜 썸네일 제거, 홈 6건 페이징, 전체 목록 4종 정렬, 빈 상태 CSS 수정
- [x] 공개 블로그와 유튜브 카테고리 분리 및 헤더 법적 상호 적용
- [x] 승인된 공식 로고 PNG 원본 보존, 화면용 WebP 최적화 및 헤더·푸터·Open Graph 적용
- [x] FAQ와 서버 비저장 문자 상담 작성 화면 통합
- [ ] 실제 저장형 상담게시판의 DB·스팸 방어·개인정보 보유/삭제 정책 승인

| 항목 | 내용 |
|---|---|
| 문서 ID | HRE-ADMIN-PROGRESS-001 |
| 작성 기준일 | 2026-08-26 |
| 기준 설계서 | `리더스시티 행복한부동산 관리자시스템 설계서.md` v1.3 |
| 현재 상태 | 동일 Worker·Access·GitHub master 자동 배포 연결 완료, 관리자 운영 쓰기 활성화 |
| 구현 진행도 | 부동산뱅크 가져오기·네이버 매물 직접 관리·홈·블로그·유튜브·지역 설명 수직 기능과 GitHub 저장 코드 완료 |
| 현재 완료 범위 | 동일 Worker Access 검증, Origin·CSRF·콘텐츠·SHA 검증, 부동산뱅크 파서·개인정보 제거·허용 JSON·WebP 저장 코드 완료 |

## 1. 설계 검토 판정

**판정: 조건부 승인**

화면, 입력 필드, 데이터 스키마, API, GitHub SHA 충돌, 보안 위협, 배포 실패, 롤백, 테스트와 운영 절차까지 연결되어 있어 개발 기준으로 사용할 수 있다. 편집·저장 코드는 구현했으며, 아래 미완료 항목은 Production Secret 연결과 실제 운영 저장을 시작하기 전에 확정한다.

### 반드시 보완하거나 결정할 사항

- [x] **배포 제품 확정** — Workers Static Assets와 관리 API를 같은 Worker로 배포한다.
- [x] **제품별 설계 통일 기준 확정** — Pages 전용 설명보다 ADR-004와 실제 Worker 설정을 우선한다.
- [x] **인증 방식 확정** — Cloudflare Access, 정확한 허용 이메일 2개와 Email OTP를 사용한다.
- [ ] **GitHub 직접 쓰기 검증** — 승인된 테스트 브랜치 쓰기·복원은 확인했다. 남은 항목은 `master` 보호 정책과 운영 브랜치 직접 커밋 허용 여부다.
- [ ] **현재 JSON 마이그레이션 명시** — `office.introduction` 등 실제 필드와 설계 필드의 매핑, 신규 `updatedAt`, 이미지 필드의 도입 순서를 확정한다.
- [ ] **Public draft 위험 승인** — `draft`도 Public GitHub 저장소에서 공개된다는 운영 원칙을 관리자 교육과 서버 검증에 반영한다.

### 현재 확인된 기반

- [x] Astro 7 정적 홈페이지 구현
- [x] `src/data/*.json` 기반 공개 콘텐츠 구조
- [x] 공개 매물 상태·가격·금지 필드 기본 검증
- [x] `npm test`, `npm run check`, `npm run build` 명령 구성
- [x] GitHub Actions CI 구성
- [x] Cloudflare Workers Static Assets용 `wrangler.jsonc` 구성
- [x] 운영 도메인 `https://leaderscityhappy.com` 확정
- [x] 관리자시스템 상세 설계서 v1.0 작성
- [x] 관리자 UI 디렉터리와 `/admin/`, `/admin/listings/` 읽기 전용 화면 구현
- [x] 동일 Worker 관리 API 진입점과 읽기 전용 상태 API 구현
- [x] Cloudflare Access 애플리케이션·Include 정확한 이메일 2개·Require One-time PIN·OTP 전용 로그인 설정 확인
- [x] GitHub Contents API 허용 파일 조회·SHA 비교·커밋 코드 구현
- [x] Production Worker에 Fine-grained token과 테스트 저장소·브랜치 Secret 연결 및 실제 커밋·복원 시험
- [ ] R2 이미지 관리 없음

### 현재 기준선 검증

- [x] `npm test` 정상화 — 2026-08-25 기준 51/51 통과
- [x] 신규 지역 필드(`district`, `neighborhoodSlug`, `neighborhoodName`, `complexName`) 테스트 fixture 반영
- [x] 신규 사무소 필드(`serviceArea`, `publicClaims`) 테스트 fixture 반영
- [x] `npm run build` 정상화 — Astro·TypeScript 오류 0개
- [x] 단지 전체 비교·상세 페이지와 현재 단지 스키마 일치
- [x] 매물 페이지와 현재 매물 스키마 일치
- [x] `npx wrangler deploy --dry-run`에서 정적 자산과 Worker 번들 확인
- [x] 운영 도메인과 테스트 Worker 공개 홈 `200`, 비로그인 관리자 UI·API `302` Access 리디렉션, Worker Secret 3종 등록 확인
- [x] 운영 도메인 관리자 대시보드·same-worker API 정상 상태와 canonical·검색 허용 설정 브라우저 검증

Cloudflare 연결 기록(2026-08-25):

- 운영 주소: `https://leaderscityhappy.com`, 관리자 주소: `https://leaderscityhappy.com/admin/`
- 보조 확인 주소: `https://leaders-city-happy-realty.k4858678.workers.dev`
- 최초 Git 자동 배포 버전: `5e3350e4-276a-46a6-a696-ff0dc9ad0927`
- 운영 쓰기 활성 버전: `1e5f54e2-85cc-4123-b451-6e3f51e8465d`
- GitHub 시험 브랜치: `admin-storage-test`, 운영 저장 브랜치: `master`
- 저장 시험: `631cc22`, 원상복구: `44afe18`, 기준 내용 일치 확인
- 운영 브랜치 `master`: fast-forward 병합 후 자동 배포 트리거 `0f07589`
- Cloudflare Git 빌드 `91d02fd6` 및 GitHub Actions `CI #3` 성공
- 보호 대상: 두 호스트의 `/admin*`, `/api/admin*`
- 정책 조합: Include `Email` 2개 OR 묶음 + Require `Login method: One-time PIN` AND 조건
- 로그인 화면: Email 입력과 `Send login code` 노출 확인, 허용 이메일 1개의 실제 OTP 수신·관리자 진입 성공을 운영자가 확인
- 남은 인증 검증: 다른 허용 이메일 로그인과 비허용 이메일 차단 확인
- 기존 Pages `real-estate-website`는 운영 도메인 연결만 해제하고 `real-estate-website-dnv.pages.dev`를 롤백용으로 유지

## 2. 상태 표기와 완료 원칙

- `[x]`: 파일, 테스트 결과, 배포 기록 또는 운영자 승인으로 확인한 완료
- `[ ]`: 미착수, 진행 중 또는 확인 대기
- 각 단계는 구현만으로 완료하지 않는다.
- 각 단계의 테스트, 보안 점검, 문서 갱신, 롤백 시험을 모두 마쳐야 다음 단계로 진행한다.
- 사용자 입력과 외부 계정 설정이 필요한 항목은 추정값으로 완료 처리하지 않는다.

## 3. 0단계 — 아키텍처와 운영 기준 확정

### 0-1. 현재 운영 환경 확인

- [x] Cloudflare 대시보드에서 운영 Worker와 롤백용 Pages 프로젝트 확인
- [x] Pages 리소스명·롤백 주소와 운영 Worker 배포 버전 기록
- [x] 기존 Pages의 GitHub 연결과 Production 브랜치 `master` 확인
- [x] 빌드 명령 `npm run build`와 Worker 배포 명령 `npx wrangler deploy` 확인
- [x] 출력 디렉터리 `dist` 확인
- [x] `leaderscityhappy.com` Worker Custom Domain과 별도 `workers.dev` 보조 주소 확인
- [x] apex `https://leaderscityhappy.com`을 canonical 도메인으로 확정하고 운영 HTML에서 확인
- [ ] Preview 검색 색인 차단 확인

### 0-2. 배포 아키텍처 결정

- [x] 현재 Workers Static Assets를 유지하고 Worker script로 `/api/admin/*`를 처리
- [ ] 대안 검토: Cloudflare Pages로 전환하고 루트 `functions/` 사용
- [x] Worker Git 연동과 Production 자동 배포 시험, Production 이외 브랜치 빌드 비활성화
- [x] ADR에 선택 이유, 비용, 변경 파일, 롤백 방법 기록
- [x] `CODEX.md`, `README.md`, `docs/operations/CLOUDFLARE_DEPLOY.md`, `wrangler.jsonc`의 설명 통일
- [x] 관리자 API 최소 시험 경로가 운영 Custom Domain과 보조 Worker 주소에서 Access로 보호되는지 확인

완료 기준: 저장소 설정, 운영 문서, Cloudflare 대시보드가 동일한 배포 제품과 흐름을 가리킨다.

### 0-3. 인증 방식 결정

- [x] Cloudflare Access One-time PIN ID 공급자 추가와 OTP 전용 로그인 적용
- [x] 서로 다른 허용 이메일을 정확히 2개로 제한하기로 결정
- [x] 실제 허용 이메일 2개를 Worker Secret에 등록
- [ ] 두 이메일 계정 자체 MFA 적용 확인
- [ ] 비상용 추가 관리자 필요 여부 결정
- [ ] Access 세션 시간 확정
- [ ] 중요 정보 변경 시 최근 인증 기준 확정
- [x] `/admin*`, `/api/admin*` 보호 범위와 공개 홈 비보호 시험
- [ ] Preview 관리자 경로도 Access로 보호
- [x] Bypass 정책이 없는지 확인

완료 기준: 비로그인·비허용 계정은 UI와 API 모두 접근할 수 없고 승인된 관리자는 PC와 모바일에서 로그인할 수 있다.

### 0-4. GitHub 쓰기 정책 결정

- [ ] Fine-grained PAT 발급자와 교체 담당자 확정
- [x] 저장소 한 개, Contents Read/Write만 허용
- [x] Workflows Write 등 불필요한 권한 미부여
- [x] 토큰 만료기간 결정 — 운영자 요청에 따라 만료 없음, 미사용·유출 의심 시 수동 폐기
- [x] 검증은 `admin-storage-test`, 운영 전환은 검증·병합 후 `master`로 분리
- [x] 승인된 테스트 브랜치에서 읽기·쓰기·원상복구 시험
- [ ] `master` 대상 관리자 Contents API 저장 후 자동 배포 전체 흐름 최종 확인
- [x] 토큰 유출 시 즉시 폐기·재발급하고 Worker Secret을 교체하는 절차 작성

완료 기준: 운영 파일을 수정하지 않는 시험에서 허용 파일 쓰기, 금지 경로 차단, SHA 충돌이 확인된다.

### 0-5. 운영 정책 입력

- [ ] 매물 최근 확인 유효기간
- [ ] 유효기간 초과 시 경고 또는 공개 차단 정책
- [ ] 계약완료·종료 매물 URL 유지 여부
- [ ] 사진 없는 매물 공개 여부
- [ ] 매물당 최대 사진 수
- [ ] 매물 ID와 slug 생성·변경 정책
- [ ] 실제 후기와 공개 동의 근거
- [ ] 초기 등록 단지와 추가할 동구 주요 단지의 정보 출처와 확인 기준일
- [ ] 개인정보처리방침과 이용 안내 최종 문안
- [ ] 장애 알림 채널과 긴급 롤백 담당자

## 4. 1단계 — 안전한 관리자 기반과 매물 관리

### 1-1. 공통 도메인과 검증 모듈

- [ ] 현재 `scripts/content-validation.mjs` 테스트를 기준선으로 고정
- [ ] 콘텐츠 타입, 정규화, 검증, 금지 내용 검사를 런타임 중립 모듈로 분리
- [ ] Node 빌드와 Cloudflare 런타임에서 같은 검증 규칙 사용
- [ ] 현재 JSON 필드와 신규 필드의 호환 규칙 작성
- [ ] 날짜, 금액, 면적, ID, slug, 상태 전이 검증 구현
- [ ] 전화번호·정확한 동·호수·토큰·계약정보 패턴 검사 구현
- [ ] 기존 검증 회귀 테스트 통과

### 1-2. 관리자 UI

- [x] 관리자 공통 레이아웃과 한글 내비게이션 구현
- [x] 대시보드와 시스템 정보 화면 구현
- [x] 관리자 경로를 sitemap과 `llms.txt`에서 제외
- [x] 관리자 경로 `noindex`, `no-store`, `X-Robots-Tag` 적용
- [ ] 관리자 전용 CSP 분리 검토 — 현재는 공개 사이트와 동일한 제한적 전역 CSP 사용
- [x] Secret과 서버 설정 원문이 화면에 표시되지 않도록 허용 목록 적용
- [ ] 360·390·430px과 키보드 탐색 기본 확인

구현·검증 기록(2026-08-25):

- `/admin/` 대시보드, `/admin/listings/` 목록과 0건 상태 구현
- 매물 등록·수정, 첫 화면·사무소, 블로그·유튜브, 지역·단지 설명 편집 화면 구현
- Chrome 데스크톱과 도구 최소 폭 500px에서 가로 넘침 없음 확인
- 모바일 메뉴 열기·닫기와 닫은 뒤 메뉴 버튼 포커스 복귀 확인
- 매물 검색·상태 필터의 URL 복원·갱신·초기화와 결과 수 `aria-live` 확인
- 360·390·430px 및 실제 Android Chrome·iPhone Safari는 별도 확인 필요

### 1-3. 인증·보안 API 기반

- [x] 관리자 API 라우팅과 공통 응답 기반 구현
- [x] Access JWT 서명, issuer, audience, 만료, 허용 이메일 2개 검증
- [x] HMAC CSRF, 동일 Origin, JSON Content-Type, HTTP 메서드 검증
- [x] 기본 설정에서는 모든 콘텐츠 쓰기를 fail closed로 비활성화
- [ ] 요청 크기 제한 완료, Rate Limit은 Production 자원 결정 후 적용
- [ ] 개인정보 없는 구조화 로그 구현
- [x] `X-Request-Id` 응답과 Secret 누락 시 fail closed 구현

### 1-4. GitHub Contents API 공통 계층

- [x] 소유자, 저장소, 브랜치를 서버 설정으로 고정
- [x] 수정 가능한 `src/data/*.json` 경로를 서버 허용 목록으로 고정
- [x] 최신 파일 SHA 조회와 UTF-8 JSON 파싱 구현
- [x] 최신 SHA 비교와 `409 Conflict` 구현
- [x] 안정적인 2칸 JSON 직렬화와 마지막 줄바꿈 적용
- [x] 콘텐츠 요청 한 번에 GitHub JSON 쓰기 한 번만 실행
- [ ] 429, `Retry-After`, 5xx 재시도 정책 구현
- [x] 토큰·관리자 이메일·본문을 커밋 메시지와 응답에 넣지 않음

### 1-5. 매물 관리 수직 기능

> 아래 자체 `Listing` 상태 폼 항목은 레거시 기록이다. 현재 운영 매물은 `naver-listings.json`을 대상으로 부동산뱅크 가져오기와 네이버 공개 매물 직접 등록을 사용한다.

- [x] 저장소 기준 읽기 전용 매물 목록과 0건 상태 구현
- [x] 매물 검색과 상태 필터 구현
- [x] 신규 등록·수정 폼 구현, 복제는 미구현
- [ ] 억·만원 입력과 원 단위 안전 정수 변환 구현
- [x] draft·published·contracted·ended 상태 입력과 공개 필터 구현
- [ ] 공개 전 미리보기와 변경 요약 구현
- [x] published 필수값, 출처, 확인일, 금지정보 공개 게이트 구현
- [ ] 저장 중 중복 클릭 차단 완료, 페이지 이탈 경고 미구현
- [ ] 저장 완료와 홈페이지 반영 완료 상태를 분리 표시

구현·검증 기록(2026-08-25):

- 매물명·위치·ID·거래유형·상태·특징 통합 검색 구현
- `draft`·`published`·`contracted`·`ended` 상태 필터와 검색어 조합 구현
- `q`, `status` URL 쿼리 복원·갱신과 잘못된 상태값 안전 초기화 구현
- 필터 단위 테스트 5건, 관리자 화면 회귀 테스트 1건 추가

### 1단계 검증 게이트

- [ ] 허용 관리자만 로그인 가능
- [x] 비로그인 URL 직접 호출 시 API가 Access로 리디렉션되어 우회 불가
- [x] JWT·CSRF·Origin 실패 단위 테스트 통과, Content-Type 실배포 확인 대기
- [ ] 매매·전세·월세 정상 및 오류 조합 테스트 통과
- [x] 오래된 SHA는 GitHub 충돌 응답을 `409`로 매핑
- [ ] draft가 공개 목록·상세·사이트맵에 나타나지 않음
- [ ] 관리자 저장 커밋 후 선택한 Cloudflare 배포가 시작됨
- [ ] 기존 공개 홈페이지 회귀 테스트 통과
- [ ] 운영자가 모바일에서 첫 draft 저장 성공

## 5. 2단계 — 전체 공개 콘텐츠 관리

### 2-1. 사무소와 중요 법적 정보

- [ ] 사무소 정보 폼과 API 구현
- [ ] 실제 `office.json`의 `serviceArea`, `email`, `introduction`, `publicClaims` 등 기존 필드 보존
- [ ] 공개 운영 현황 변경 시 운영 기준·표시값·변경 이력 확인
- [ ] 상호·대표자·전화·주소·등록번호 변경 시 강한 확인
- [ ] 구조화 데이터와 여러 공개 페이지에 미치는 영향 미리보기
- [ ] 최근 인증이 필요한 중요 변경 정책 구현

### 2-2. 단지·외부 콘텐츠·FAQ·후기

- [x] 대전 동구 주요 단지 추가·수정 폼과 사진·면적·공급·생활환경·시설 상태·FAQ·관련 콘텐츠·복수 공개 출처·확인일 검증
- [x] 리더스시티 전체 숫자 카드·4·5블록 비교·공통 확인사항을 `complexes-overview.json`과 관리자 폼에서 관리
- [x] 네이버 블로그·유튜브 링크·공개 상태·썸네일 관리와 최신순 블로그 9개·유튜브 6개 단위 페이징
- [x] 블로그·유튜브 관리 카테고리 분리, 카테고리별 건수와 제목·URL 검색
- [x] YouTube 일반 영상·Shorts 형식 선택과 공개 화면 전체·일반 영상·Shorts 필터형 단일 목록 구현
- [x] HTTPS와 네이버 블로그·유튜브 허용 도메인 검증
- [x] 관리자 API와 분리된 공식 RSS `workflow_dispatch` 신규 항목 append-only 동기화 구현
- [ ] Repository Variable·수동 워크플로·Production 반영 확인 후 예약 실행 승인
- [x] 네이버 공개 매물 스냅샷을 관리자 쓰기 허용 목록에 연결하고 부동산뱅크 가져오기·수동 등록으로 관리
- [x] URL 미리보기 SSRF·허용 리디렉션·응답 크기·시간 제한 구현
- [ ] FAQ 등록·정렬·비공개 보관
- [ ] 후기 공개 동의·개인정보 검수 게이트

### 2-3. 홈페이지 공통 문구와 정책

- [x] `home-content.json`에 대표·사무소·리더스시티 홈 문구 스키마 정의
- [ ] `policies.json` 구조화 본문 스키마 정의
- [x] raw HTML 없이 홈 대표·사무소·리더스시티 설명 관리
- [ ] 개인정보처리방침과 이용 안내 편집·미리보기
- [x] 기준 사이트 핵심 홈 문구를 `home-content.json`으로 1차 마이그레이션
- [ ] title, description, canonical, 구조화 데이터 회귀 테스트

### 2단계 검증 게이트

- [ ] 모든 대상 콘텐츠를 관리자 폼으로 관리 가능
- [ ] 중요 법적 정보는 추가 확인 없이 저장 불가
- [ ] 출처 없는 단지 수치가 공개되지 않음
- [ ] 개인정보 검수 없는 후기가 공개되지 않음
- [ ] 금지 HTML, XSS, SSRF 테스트 통과
- [ ] 공개 페이지의 모바일·SEO·접근성 회귀 없음

## 6. 3단계 — 이미지, 배포 이력과 복구

### 3-1. 이미지 처리 방식 확정

- [ ] R2 사용과 공개 Custom Domain 승인
- [x] 초기 비용 0원을 위해 브라우저 WebP 변환 후 GitHub 공개 이미지 경로 저장
- [ ] 비용 0원 조건과 유료 전환 기준 결정
- [ ] HEIC·AVIF 지원 시점 결정
- [ ] 원본 영구 미보관 정책 승인
- [ ] 이미지 권리와 개인정보 검수 책임자 지정

### 3-2. 안전한 이미지 업로드

- [x] 브라우저 JPEG·PNG·WebP 입력, 서버 WebP만 허용, 처리 결과 2MB 제한 구현
- [ ] MIME, magic byte, 픽셀 수, 애니메이션, SVG 검사
- [x] Canvas WebP 재인코딩으로 EXIF·GPS 제거, 서버 RIFF/WEBP와 크기 재검증
- [ ] R2 staging → 콘텐츠 JSON 커밋 → public 승격 흐름 구현
- [ ] 커밋 실패 시 staging 정리와 보상 처리
- [x] 대표 이미지와 대체 텍스트 관리, 다중 이미지 순서·해시는 후속
- [ ] 사용 중 이미지 삭제 차단과 orphan 정리
- [ ] R2 이미지 도메인을 CSP에 제한적으로 추가

### 3-3. 배포 상태·변경 비교·복구

- [ ] GitHub commit SHA와 선택한 Cloudflare 배포 상태 연결
- [ ] 대기·빌드·배포·성공·실패·확인 불가 상태 구분
- [ ] 마지막 정상 배포 표시
- [ ] 커밋별 허용 JSON 변경 이력과 필드 차이 표시
- [ ] 이전 내용을 새 커밋으로 복원
- [ ] 강제 push 없이 소스와 배포를 함께 복구
- [ ] 배포 실패와 상태 조회 실패를 구분

### 3단계 검증 게이트

- [ ] 원본 이미지가 Public GitHub와 R2에 남지 않음
- [ ] EXIF·GPS 포함 사진이 처리 없이 저장되지 않음
- [ ] R2 장애와 GitHub 커밋 실패 복구 테스트 통과
- [ ] 실패한 배포가 성공으로 표시되지 않음
- [ ] 이전 JSON 버전을 새 커밋으로 복원 가능
- [ ] 공개 배포 롤백 후 소스도 정상 버전으로 일치

## 7. 출시 전 통합 검증

### 기능과 데이터

- [ ] 매물 등록·수정·복제·상태 변경 E2E 통과
- [ ] 전체 콘텐츠 관리 E2E 통과
- [ ] SHA 충돌과 네트워크 실패 시 입력 보존
- [ ] API와 빌드가 동일한 검증 규칙 사용
- [ ] 공개 승인 콘텐츠만 Public 저장소에 존재

### 보안

- [x] 비로그인 관리자 UI·API 접근 차단
- [ ] 허용되지 않은 계정 차단
- [ ] JWT 위조, 만료, audience 불일치 차단
- [ ] CSRF, XSS, SSRF, 경로 조작 시험 통과
- [ ] GitHub 토큰과 Access JWT가 번들·응답·로그에 없음
- [ ] Preview 쓰기 비활성화
- [x] 토큰 최소 권한과 폐기·회전 절차 확인

### 품질과 운영

- [x] `npm test` 통과 — 42/42
- [x] `npm run check` 통과 — 오류·경고 0건
- [x] `npm run build` 통과 — 정적 페이지 19개 생성
- [x] Cloudflare Worker 로컬 빌드와 Production Git 자동 배포 검증 통과, Preview는 비활성화
- [ ] 404, robots, sitemap, canonical 회귀 테스트 통과
- [ ] Chrome·Edge·Safari·Firefox 확인
- [ ] Android Chrome 실제 기기 확인
- [ ] iPhone Safari 실제 기기 확인
- [ ] 키보드·포커스·화면 읽기 프로그램 기본 점검
- [ ] 관리자 로그인·저장·배포 확인·복구 운영 문서 작성
- [ ] 운영자가 첫 저장과 복구를 직접 수행
- [ ] 배포 실패 대응과 긴급 롤백 담당자 승인

## 8. 4단계 — 조건 충족 시 D1 전환 검토

다음 항목은 초기 개발 범위가 아니다.

- [ ] 2개월 연속 월 2회 이상 SHA 충돌
- [ ] 관리자 3명 이상
- [ ] Public 저장소에 둘 수 없는 비공개 초안 필요
- [ ] 하루 수십 회 이상 저장 또는 월 500회 빌드 한도 접근
- [ ] 여러 JSON 파일의 원자적 저장 필요
- [ ] GitHub API 장애가 반복적으로 업무를 방해
- [ ] 운영자 승인과 별도 D1 전환 ADR 작성
- [ ] 데이터 마이그레이션·백업·복구·롤백 시험

## 9. 진행 기록 규칙

1. 완료 항목 아래에 완료일, 담당자, 커밋 SHA, 검증 명령 또는 외부 설정 증거를 기록한다.
2. Cloudflare 대시보드, Access, GitHub 토큰과 같은 외부 설정은 화면 확인 없이 완료 처리하지 않는다.
3. 설계와 실제 구현이 달라지면 먼저 ADR과 이 체크리스트를 갱신한다.
4. 사용자 변경 파일과 관련 없는 리팩터링 또는 포맷 변경을 섞지 않는다.
5. 실행하지 않은 테스트와 실기기 검수를 통과로 표시하지 않는다.
