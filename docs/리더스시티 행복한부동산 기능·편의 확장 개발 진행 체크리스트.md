# 리더스시티 행복한부동산 기능·편의 확장 개발 진행 체크리스트

| 항목 | 내용 |
|---|---|
| 문서 ID | HRE-EXPAND-PROGRESS-001 |
| 버전 | v0.2 |
| 작성 기준일 | 2026-08-31 |
| 현재 상태 | 1차 고객 기능과 관리자 변경 요약·배포 상태·변경 이력·안전 복원 로컬 구현 완료, 실기기·Production 반영 대기 |
| 기준 설계서 | `리더스시티 행복한부동산 기능·편의 확장 개발 설계서.md` |
| 기본 아키텍처 | Astro 정적 사이트 + GitHub JSON + 동일 Cloudflare Worker 관리 API |

> 완료 표시는 코드 작성만으로 하지 않는다. 단위 테스트, 정적 검사, Production 빌드, 화면 검수와 운영 반영 증거까지 확인된 경우에만 `[x]`로 바꾼다. 외부 계정이나 실기기가 필요한 항목은 실제 확인 전 완료 처리하지 않는다.

---

## 1. 현재 진행도

| 단계 | 상태 | 완료 기준 |
|---:|---|---|
| 0. 운영 기준 확정 | 진행 중 | 고객 제한값 확정, 경고 기준·통계 범위는 대기 |
| 1. 공통 순수 로직 | 완료 | 파서·query·관심·비교·문의 모듈 단위 테스트 통과 |
| 2. 조건 검색·관심 매물 | 진행 중 | 로컬 자동 검증 완료, 실제 Android·iPhone 확인 대기 |
| 3. 매물 비교·문의 도우미 | 진행 중 | 로컬 자동 검증 완료, 실제 문자·카카오 앱 확인 대기 |
| 4. 관리자 변경 요약·사전 검증 | 로컬 완료 | diff·동일 서버 검증·이탈 경고·390px 증적 완료, Production 반영 대기 |
| 5. 배포 상태 확인 | 로컬 완료 | marker v2·digest·상태 API/UI 자동 검증 완료, Production 반영 대기 |
| 6. 변경 이력·안전 복원 | 로컬 완료 | 이력·diff·검증·새 커밋 복원 자동 테스트 완료, 승인 콘텐츠 Production E2E 대기 |
| 7. 매물 재확인 대기열 | 로컬 완료 | 상태·정책·Bank 갱신·필터·선택 확인·이력 자동 검증 완료, Production 반영 대기 |
| 8. 최소 이용 통계 | 보류 | 개인정보 없는 기본 분석 필요성이 확인된 경우에만 적용 |
| 9. 통합 품질·운영 검수 | 진행 중 | 자동 검사 완료, 실기기·Production 검증 대기 |

### 상태 표기

- `완료`: 증적까지 확인됨
- `진행 중`: 구현 또는 검증이 일부 진행됨
- `대기`: 운영자 결정이나 외부 확인 필요
- `미착수`: 아직 시작하지 않음
- `보류`: 필요성을 다시 확인한 뒤 진행
- `제외`: 이번 확장 범위에서 의도적으로 제외

---

## 2. 현재 기준선과 보존 조건

### 2.1 확인된 기준선

- [x] 기능·편의 확장 상세 설계서 작성
- [x] 공개 매물은 `src/data/naver-listings.json`을 중심으로 정적 생성
- [x] 현재 매물 목록에 거래유형·매물유형 필터와 정렬 기능 존재
- [x] 관리자 JSON 저장은 GitHub 단일 branch-tip snapshot과 non-force ref 갱신 사용
- [x] 공개 사이트는 DB 없이 정적으로 운영
- [x] 관리자 API만 `/api/admin/*`에서 Worker 우선 실행
- [x] 전화·문자·카카오 상담 내용을 서버에 저장하지 않는 정책 확정

### 2.2 반드시 보존할 기존 동작

- [x] 기본 정렬은 `최근 등록순`으로 유지
- [x] 필터 초기화 시 첫 페이지와 최근 등록순으로 복귀
- [x] 매물 카드의 네이버 개별 링크와 중개사 매물 링크 유지
- [x] 네이버에 공개되지 않은 정확한 호수·고객·소유자 정보 미저장
- [x] `published` 이외 매물과 관리자 경로의 검색 색인 차단 유지
- [x] 360px부터 가로 넘침 없는 모바일 레이아웃 유지
- [x] 전화·문자·카카오 CTA와 FAQ 문자 작성 흐름 유지
- [x] 부동산뱅크·외부 콘텐츠 예약 동기화의 허용 경로와 fail-closed 정책 유지
- [x] `ADMIN_WRITE_ENABLED`와 Secret 누락 시 관리자 쓰기 차단 유지

---

## 3. 0단계 — 운영 기준 확정

### 3.1 고객 기능 제한값

- [x] 관심 매물 최대 30개 승인
- [x] 비교 선택 최대 3개 승인
- [x] 종료·삭제된 관심 매물 ID는 현재 공개 목록을 읽을 때 정리
- [x] 관심 매물은 같은 브라우저에만 저장된다는 안내 문구 승인
- [x] 비교 URL에 공개 매물 ID만 포함한다는 원칙 확인

### 3.2 필터 표시 기준

- [x] 매매 가격 범위는 만원 단위
- [x] 전세 보증금 범위는 만원 단위
- [x] 월세 보증금·월 임대료 범위를 만원 단위의 독립 조건으로 처리
- [x] 면적 필터를 첫 번째 공개 표시면적의 ㎡ 기준으로 처리
- [x] 가격·면적 범위의 최소·최대 역전 시 해당 범위를 해제하고 안내
- [x] 잘못된 URL query는 무시하고 안전한 기본값으로 복귀

### 3.3 운영 기능 기준

- [ ] 매물 재확인 경고 일수 결정 또는 기능 비활성 유지
- [ ] 경고만 제공하고 자동 비공개·자동 삭제하지 않는 정책 확인
- [ ] 복원 권한과 2단계 확인 문구 승인
- [ ] 변경 이력 조회 기간 또는 최대 건수 결정
- [ ] 단일 Worker 버전 100% 배포를 상태 판정 기준으로 사용하는 조건 확인

### 3.4 통계 범위

- [ ] 현재 Cloudflare Web Analytics 활성 여부 확인
- [ ] 기본 방문 통계만으로 충분한지 판단
- [ ] 별도 행동 이벤트가 실제 운영에 필요한지 판단
- [ ] 즐겨찾기 ID·검색어·문의 문장·전화번호를 분석값에 넣지 않는 원칙 확인
- [ ] custom event가 불필요하면 8단계 전체 보류

완료 기준: 설계 기본값과 운영자가 결정해야 하는 값이 분리되고, 미확정 값을 코드에 임의로 넣지 않는다.

---

## 4. 1단계 — 공통 순수 로직

### 4.1 가격·면적과 query

- [x] 현재 가격·면적 파서를 재사용 가능한 순수 모듈로 분리
- [x] 매매·전세·월세별 비교 가격 기준 구현
- [x] 월세 보증금과 월 임대료를 독립 조건으로 처리
- [x] 면적 문자열의 첫 번째 공개 표시면적 파싱 규칙 유지
- [x] 허용 query 이름·형식·숫자 상한 정의
- [x] 숫자 범위·enum·매물 ID query 정규화
- [x] 알 수 없는 query 제거 또는 무시
- [x] 기본값은 URL에서 생략

### 4.2 관심·비교 상태

- [x] `localStorage` 저장 키와 schemaVersion 정의
- [x] 관심 매물 ID 배열 중복 제거
- [x] 최대 개수 초과 차단과 사용자 안내
- [x] 존재하지 않거나 종료된 ID 정리
- [x] localStorage 접근 실패 시 메모리 상태로 안전하게 동작
- [x] 비교 ID 최대 3개 제한
- [x] 비교 URL의 순서·중복·잘못된 ID 정규화

### 4.3 문의 문장

- [x] 공개 필드만 입력받는 문의 문장 생성 함수 구현
- [x] 거래유형·가격·매물번호와 희망 조건의 안정적인 표시 순서 정의
- [x] 자유 입력값은 브라우저 메모리에서만 유지
- [x] 문자 URL에는 본문을 넣지 않고 복사 후 붙여넣기 방식 사용
- [x] 카카오 링크는 승인된 공식 URL만 사용
- [x] Clipboard API 실패 시 선택·복사 fallback 제공

### 4.4 단위 테스트

- [x] 매매·전세 가격과 억·만원 파싱 테스트
- [ ] 매매·전세 최소·최대 경계 조합 추가 테스트
- [x] 월세 보증금·월 임대료 조합 테스트
- [x] 면적 누락·복수 면적·잘못된 문자열 테스트
- [x] query 정규화와 잘못된 값 테스트
- [x] 관심 매물 중복·30개 경계·종료 ID 테스트
- [x] 비교 최대 3개·4번째 차단과 중복 ID 테스트
- [x] 문의 문장에 공개 필드만 포함되는 테스트

완료 기준: DOM 없이 모든 핵심 규칙을 테스트할 수 있고, 기존 정렬·가격·면적 결과와 회귀가 없다.

---

## 5. 2단계 — 조건 검색과 관심 매물 UI

### 5.1 조건 검색

- [x] 기존 거래유형·매물유형 필터 유지
- [x] 거래유형에 맞는 가격 필터만 표시
- [x] 가격 최소·최대 입력 구현
- [x] 면적 최소·최대 입력 구현
- [x] 선택된 조건 요약 chip 구현
- [x] 빠른 조건 버튼을 실제 공개 데이터 범위에 맞게 제공
- [x] 적용·초기화 버튼을 44×44px 이상으로 구현
- [x] 필터 결과 수를 `aria-live`로 안내
- [x] 결과 0건에서 조건 초기화와 문의 조건 작성 제공

### 5.2 모바일 필터

- [x] 767px 이하에서 접이식 필터 패널 제공
- [x] 열기·닫기 상태를 `aria-expanded`로 전달
- [x] 패널과 컨트롤 관계를 `aria-controls`로 연결
- [x] 조건 적용 후 필터 버튼으로 포커스 복귀
- [ ] 화면 확대·가상 키보드에서도 적용·초기화 버튼 접근 가능
- [x] 360px 자동 검증에서 가로 넘침 없이 패널 사용

### 5.3 URL 상태

- [x] 필터·정렬을 허용 query에 반영
- [ ] 뒤로가기·앞으로가기로 이전 조건 복원
- [x] 공유 URL 진입 시 같은 조건 재현
- [x] 기본 목록 canonical 유지
- [x] 필터 query를 sitemap에서 제외

### 5.4 관심 매물

- [x] 각 카드에 의미가 명확한 관심 추가·해제 버튼 제공
- [x] 버튼 상태와 스크린리더 이름 동기화
- [x] 관심 매물 개수 표시
- [x] 관심 매물만 보기 구현
- [x] 새로고침 후 같은 브라우저에서 상태 유지
- [x] 저장 불가 환경에서도 매물 탐색 기능은 유지
- [x] 공개 매물 ID만 브라우저에 저장된다는 짧은 안내 제공

완료 기준: 360px 모바일과 키보드에서 조건 설정·초기화·관심 저장을 완료할 수 있고 같은 URL로 결과를 재현할 수 있다.

---

## 6. 3단계 — 매물 비교와 문의 도우미

### 6.1 비교 선택과 tray

- [x] 카드에서 비교 선택·해제 제공
- [x] 현재 선택 개수와 최대 3개 제한 표시
- [x] 4번째 선택을 차단하고 이유 안내
- [x] 선택 tray를 모바일 상담 CTA 위에 sticky 배치
- [x] 비교 시작과 전체 해제 버튼 제공
- [x] 현재 공개 목록에 없는 매물 ID는 비교 대상에서 제외

### 6.2 비교 페이지

- [x] `/properties/compare/?ids=...` 정적 페이지 구현
- [x] 매물명·매물번호·거래유형·가격·면적·층·방향·등록일·매물유형 비교
- [x] 매물별 네이버 원문 링크 제공
- [x] 모바일은 카드형 세로 비교로 전환
- [x] 데스크톱 표의 `th` scope와 읽기 순서 적용
- [x] 비교 ID가 0개면 매물 목록 안내
- [ ] 비교 ID가 1개일 때 추가 선택 안내 문구 보완
- [x] 비교 query에 `noindex,follow`와 기본 canonical 적용

### 6.3 상담 조건 도우미

- [x] 선택 매물 또는 일반 조건으로 문의 문장 생성
- [x] 선택된 공개 매물번호를 생성 전 화면에서 확인 가능
- [x] 읽기 전용 결과에서 생성 문장 확인
- [x] 복사 버튼과 성공·실패 상태 제공
- [x] 본문 없는 문자 앱 링크 제공
- [x] 카카오 공식 상담 링크 연결 구현
- [x] 서버 요청과 로그 전송이 발생하지 않음
- [x] 새로고침 시 자유 입력 상담 내용이 남지 않음

완료 기준: 최대 3개 공개 매물을 모바일에서 비교하고, 공개 정보만 포함한 문장을 복사하거나 문자·카카오 상담으로 연결할 수 있다.

---

## 7. 4단계 — 관리자 변경 요약과 서버 사전 검증

### 7.1 공통 diff

- [x] 허용 리소스별 공개 필드 라벨 map 정의
- [x] 추가·수정·삭제를 구분하는 순수 diff 모듈 구현
- [x] 배열 순서 변경과 실제 데이터 변경 구분
- [x] 긴 텍스트는 안전하게 요약하고 전체 내용 펼치기 제공
- [x] Secret·인증 정보·고객 개인정보가 diff에 들어갈 수 없도록 차단

### 7.2 저장 전 화면

- [x] 저장 버튼 전에 변경 요약 단계 제공
- [x] 변경 없음 저장은 GitHub 요청 없이 종료
- [ ] 공개 페이지 구성과 가까운 부분 미리보기 제공
- [x] 저장 대상 리소스와 예상 영향 페이지 표시
- [x] 저장 중 중복 클릭 차단
- [x] 수정된 상태에서 이탈 시 브라우저 경고
- [x] 저장 성공 뒤 dirty 상태 초기화
- [x] 실패 시 입력과 diff 상태 보존

### 7.3 서버 사전 검증 API

- [x] `POST /api/admin/v1/content/validate` 라우트 구현
- [x] 기존 Access JWT·허용 이메일 검증 적용
- [x] 동일 Origin·CSRF·JSON Content-Type 검증 적용
- [x] 요청 크기와 허용 리소스 제한 적용
- [x] 실제 저장과 같은 전체 snapshot 교차 검증 모듈 사용
- [ ] 검증 성공 시 normalized diff와 영향 리소스 반환
- [ ] 검증 실패 시 필드별 오류와 안정적인 오류 코드 반환
- [x] 검증 API는 blob·commit·ref를 생성하지 않음

### 7.4 관리자 적용 순서

- [x] 네이버 매물 편집 화면 적용
- [x] 홈 콘텐츠 화면 적용
- [x] 외부 콘텐츠 화면 적용
- [x] 단지 콘텐츠 화면 적용
- [ ] FAQ·후기 등 남은 허용 리소스 적용

완료 기준: 운영자가 GitHub 커밋 전에 변경점과 공개 영향을 확인하며, 사전 검증과 실제 저장이 같은 규칙을 사용한다.

---

## 8. 5단계 — GitHub 저장과 Production 배포 상태

### 8.1 deployment marker v2

- [x] 기존 marker v1 scope reader를 2026-09-30까지 유지하고 상태 API는 v1을 `unknown`으로 안전 처리
- [x] `schemaVersion: 2` 출력 구현
- [x] source commit SHA·branch·source 종류 기록
- [x] 허용 리소스별 SHA-256 digest 생성
- [x] 검색·Bank·외부 콘텐츠·자동화 기존 scope 유지
- [x] Production build에서 commit 정보가 없으면 명확한 `unknown` 처리
- [x] marker에 Secret·관리자 이메일·원문 콘텐츠 미포함

### 8.2 빌드 환경 연결

- [x] Cloudflare Workers Builds `WORKERS_CI_COMMIT_SHA`·`WORKERS_CI_BRANCH` 환경값 연결
- [x] GitHub Actions Production-mode build의 `GITHUB_SHA`·`GITHUB_REF_NAME` 환경값 연결
- [x] Worker version metadata binding 추가
- [x] `wrangler.jsonc`의 기존 `/api/admin/*` 라우팅을 유지한 채 binding만 추가
- [ ] 단일 활성 Worker 버전 100% 조건 확인
- [ ] 점진 배포·트래픽 분할 시 `확인 불가` 처리

### 8.3 상태 API와 관리자 UI

- [x] `GET /api/admin/v1/deployment-status` 구현
- [ ] GitHub 현재 branch tip 조회
- [x] Production marker source SHA와 resource digest 조회
- [ ] 부분 완료 — 저장 완료·배포 중·공개 완료·최신 배포 포함·지연·확인 불가 분리, 명시적 Cloudflare 빌드 실패는 후속
- [x] 마지막 확인 시각 표시
- [x] 5초 자동 새로고침과 완료·지연·확인 불가 중단 조건 구현
- [x] marker·네트워크 조회 장애를 저장 실패와 구분
- [x] 모든 예약 워크플로의 marker 검사 v1·v2 호환

완료 기준: `GitHub 저장됨`과 `공개 반영됨`을 혼동하지 않고, 선택 리소스의 Production digest까지 일치해야 완료로 표시한다.

---

## 9. 6단계 — 변경 이력과 안전 복원

### 9.1 변경 이력

- [x] 허용 리소스별 Git commit history 조회 API 구현
- [x] 작성 시각·commit SHA·안전한 메시지만 표시
- [x] 최대 10건과 cursor pagination 적용
- [x] 허용 경로·현재 branch 밖 commit 내용은 조회하지 않음
- [x] 과거 blob JSON 파싱·현재 스키마 검증
- [x] 현재 버전과 과거 버전의 안전한 공개 필드 diff 제공

### 9.2 복원

- [x] 복원 대상을 현재 화면에서 명확히 표시
- [x] 복원 전 최신 branch tip과 SHA 재확인
- [x] 과거 JSON을 현재 전체 스키마로 다시 검증
- [x] 1차 변경 요약 확인 제공
- [x] 정확한 resource 이름을 포함한 2차 복원 문구 입력
- [x] force push 없이 새 commit으로 복원
- [x] ref CAS 충돌 시 쓰기 없이 `409` 반환
- [x] 복원 commit SHA 표시
- [x] 복원 resource digest를 기존 Production 상태 추적에 연결

### 9.3 복구 검증

- [ ] 정상 JSON → 과거 JSON → 현재 JSON 복원 테스트
- [x] schema 불일치 과거 버전 차단
- [x] 중간 branch 이동 충돌 테스트
- [x] GitHub API 429·5xx 처리 테스트 — 429는 재시도 가능 503, 5xx는 GitHub 장애 502로 fail closed
- [x] 배포 실패 시 소스 복원과 Cloudflare 롤백 절차 구분
- [ ] 운영자 복원 훈련과 증적 기록

완료 기준: Git 이력을 다시 쓰지 않고 과거 공개 JSON을 새 커밋으로 복원하며, Production 반영 여부까지 확인할 수 있다.

---

## 10. 7단계 — 매물 재확인 대기열

### 10.1 데이터와 기준

- [x] `registeredAt`, `checkedAt`, `lastSeenAt`, `lastReviewedAt` 의미를 문서화
- [x] 승인된 경고 일수만 설정 — 현재 미승인 상태는 `null`
- [x] 경고 미설정 상태를 정상적으로 지원
- [x] `.github/listing-review-state.json`과 `.github/listing-review-policy.json` 스키마 정의
- [x] 상태 파일에 고객·소유자·정확한 호수·내부 메모 금지
- [x] Naver ID 기준 중복·날짜 관계·출처 관계 검증

### 10.2 자동·수동 갱신

- [x] Bank sync 정상 확인 시 `lastSeenAt` 갱신
- [x] 관리자 직접 확인 시 변경 미리보기 뒤 `lastReviewedAt` 갱신
- [x] 출처 장애를 매물 미확인으로 잘못 처리하지 않음
- [x] 재확인 경고가 자동 종료·삭제를 실행하지 않음
- [x] 현재 공개 목록에서 사라진 상태와 재확인 필요 상태를 구분

### 10.3 관리자 화면

- [x] 재확인 필요 매물 개수 표시 — 미설정 정책은 숫자 대신 `기준 미설정`
- [x] 오래된 순·출처·매물유형 필터 제공
- [x] 네이버 원문 확인 링크 제공
- [x] 확인 완료 전 공통 변경 내용·서버 사전 검증 미리보기
- [x] 일괄 확인은 체크한 직접 등록 ID만 한 커밋으로 처리
- [x] 잘못된 확인을 `/admin/history/`의 `매물 재확인 상태` 새 커밋 복원으로 되돌림

완료 기준: 오래된 매물을 사람이 재확인하도록 돕되 외부 장애나 날짜 경과만으로 공개 매물을 자동 종료하지 않는다.

---

## 11. 8단계 — 최소 이용 통계

### 11.1 기본 분석

- [ ] Cloudflare Web Analytics 현재 설정 확인
- [ ] 쿠키·식별자·개인정보 처리 여부 재확인
- [ ] 홈·매물·단지 페이지 수준 지표만 우선 확인
- [ ] 기존 CSP·개인정보처리방침 영향 검토
- [ ] 통계가 없어도 사이트 핵심 기능이 정상 동작

### 11.2 별도 행동 이벤트가 승인된 경우에만

- [ ] 허용 이벤트 이름 allowlist 정의
- [ ] 요청 본문 1KB 이하 제한
- [ ] URL query·매물 ID·검색어·문의 문장 수집 금지
- [ ] 전화번호·정확한 주소·관리자 이메일 수집 금지
- [ ] 자유 문자열 payload 금지
- [ ] 집계 목적과 보유기간 문서화
- [ ] `/api/events` 추가 시 Worker 라우팅·보안·비용 별도 승인
- [ ] 개인정보처리방침과 운영 문서 동기화

완료 기준: 운영 의사결정에 필요한 최소 집계만 사용하고 방문자나 상담 내용을 식별할 수 있는 값은 수집하지 않는다.

---

## 12. 9단계 — 통합 테스트와 운영 검수

### 12.1 자동 검증

- [x] `npm test` 171개 전체 통과
- [x] `npm run check` 오류·경고 0건
- [x] `npm run build` 통과
- [x] `npm run assert:production-build` 통과
- [x] `npm run test:e2e` 7개 통과
- [x] `npm run audit:lighthouse` 통과 — 홈의 동일 CSS를 첫 문서에 인라인해 렌더 차단 왕복을 제거한 뒤 홈 97/100/100/100·LCP 2.327초, 매물 96/100/100/100·LCP 2.402초, 단지 100/97/100/100·LCP 1.652초, 전 화면 TBT 0ms·CLS 0 확인
- [x] `npx wrangler deploy --dry-run` 통과 — 정적 자산 295개 패키징

### 12.2 Playwright 핵심 흐름

- [ ] URL query로 필터 상태 복원
- [x] 필터 적용·URL 반영·초기화
- [x] 관심 추가·새로고침 유지
- [x] 최대 3개 비교와 4번째 차단
- [x] 비교 URL 복원
- [x] 문의 문장 생성과 서버 API 요청 0건
- [ ] 필터 0건 CTA·관심 삭제·Clipboard 실패 fallback E2E 보강
- [x] 관리자 변경 요약·사전 검증 — 로컬 Browser 데스크톱·390px 증적, 최종 저장 미실행
- [ ] 저장 실패 시 입력 보존
- [x] 배포 상태 전이 표시 — 로컬 데스크톱·모바일 증적은 현재 작업에서 추가
- [ ] 이력 조회와 복원 확인

### 12.3 접근성·반응형

- [ ] 360·390·430·768·1280px 확인
- [ ] 200% 확대에서 기능 손실 없음
- [ ] 키보드만으로 필터·관심·비교·문의 사용
- [ ] 포커스 표시와 열기·닫기 복귀 정상
- [ ] `aria-live`가 결과·저장 상태를 과도하게 반복하지 않음
- [ ] 비교표 또는 카드 읽기 순서 정상
- [ ] 하단 CTA·비교 tray·가상 키보드가 서로 겹치지 않음
- [x] 360px 홈·매물 화면 가로 넘침 없음

### 12.4 실기기

- [ ] Android Chrome에서 필터·관심·비교 확인
- [ ] iPhone Safari에서 필터·관심·비교 확인
- [ ] 실제 문자 앱에 생성 문장 전달 확인
- [ ] 실제 카카오 상담 링크 확인
- [ ] localStorage 비활성·용량 오류 시 핵심 탐색 유지
- [ ] 세로·가로 회전과 브라우저 뒤로가기 확인

### 12.5 Production

- [ ] 승인된 테스트 데이터로 관리자 사전 검증
- [ ] GitHub commit SHA 확인
- [ ] Cloudflare 자동 배포 시작 확인
- [ ] Production marker v2와 resource digest 확인
- [ ] 공개 URL 내용 반영 확인
- [ ] canonical·robots·sitemap 회귀 없음
- [ ] 관리자 API·화면 `noindex`, `no-store` 유지
- [ ] 직전 정상 Cloudflare 버전 롤백 확인
- [ ] 필요 시 `git revert` 후 source·Production 재일치 확인

완료 기준: 자동 검사뿐 아니라 실제 Android·iPhone, GitHub 저장, Cloudflare 배포, 공개 화면과 복구까지 확인된다.

---

## 13. 단계별 배포와 롤백

### 13.1 배포 단위

- [ ] 1차: 공통 순수 로직만 병합하고 화면 동작 회귀 확인
- [ ] 2차: 필터·관심 기능 병합
- [ ] 3차: 비교·문의 기능 병합
- [ ] 4차: 관리자 diff·사전 검증 병합
- [ ] 5차: marker v2·상태 API 병합
- [ ] 6차: history·복원 병합
- [ ] 7차: 재확인 대기열 병합
- [ ] 8차: 통계는 별도 승인 후 독립 병합

### 13.2 롤백 원칙

- [ ] 고객 기능은 신규 query·localStorage가 없어도 기본 목록이 동작
- [ ] localStorage schema 오류 시 해당 키만 안전하게 초기화
- [ ] marker v2 장애 시 공개 사이트 렌더링은 유지
- [ ] 관리자 상태 조회 장애가 콘텐츠 저장 성공으로 표시되지 않음
- [ ] 복원 기능 장애 시 기존 GitHub 저장 기능은 유지
- [ ] 잘못된 Production은 Cloudflare 직전 정상 버전으로 우선 복구
- [ ] 이어서 `git revert`로 `master`와 Production을 재일치
- [ ] 강제 push와 Git 이력 재작성 금지

---

## 14. 문서 동기화

실제 구현 단계에서 변경된 범위만 다음 문서에 반영한다.

- [ ] `CODEX.md`의 핵심 모듈·명령·공개 데이터 경계 갱신
- [ ] `01_천동_리더스시티_행복한부동산_서비스기획서.md`의 사용자 흐름 갱신
- [ ] `02_천동_리더스시티_행복한부동산_시스템설계서.md`의 데이터·API·배포 구조 갱신
- [ ] `03_천동_리더스시티_행복한부동산_개발진행체크리스트.md`의 현재 단계 갱신
- [ ] 관리자 기능 구현 시 관리자 설계서·체크리스트 갱신
- [ ] 운영 절차가 바뀌면 `docs/operations/` 관련 가이드 갱신
- [ ] 개인정보나 통계 범위가 바뀌면 개인정보처리방침 갱신
- [ ] 폐기된 필드·API·이전 marker 설명의 잔존 여부 전체 검색

---

## 15. 완료 증적 기록 형식

각 단계 완료 시 해당 항목 아래 또는 작업 보고에 다음을 남긴다.

```text
완료일:
담당자:
커밋 SHA:
변경 파일:
실행 명령과 결과:
CI 실행 URL 또는 번호:
Cloudflare 배포 버전:
Production 확인 URL:
모바일·실기기 확인:
롤백 또는 복원 확인:
남은 항목:
```

스크린샷에는 관리자 이메일, Access 토큰, GitHub 토큰, 고객 정보가 보이지 않도록 한다.

---

## 16. 다음 작업

1. 현재 로컬 변경을 승인된 배포 단위로 정리하고 Production 반영 여부를 결정한다.
2. marker v2·배포 상태·변경 이력·안전 복원 기능을 승인된 배포 단위로 Production에 반영하고 실제 resource digest 일치를 확인한다.
3. 승인 콘텐츠로 현재 저장→과거 버전 검토→새 커밋 복원→Production digest 일치 E2E와 증적을 남긴다.
4. 고객 기능의 실제 Android·iPhone과 문자·카카오 앱 연결을 확인한다.
5. 재확인 상태·정책·선택 확인 기능을 승인된 배포 단위로 Production에 반영하고 승인된 직접 매물로 저장→digest E2E를 확인한다.
6. 경고 일수는 승인 전까지 `null`로 유지하고, 개인정보 없는 통계는 실제 운영 필요가 확인된 뒤에만 진행한다.
